https://github.com/openssh/openssh-portable/pull/650

From e3b25cc3ceea670634f1798a81b36028e470a5e3 Mon Sep 17 00:00:00 2001
From: Mike Frysinger <vapier@chromiuim.org>
Date: Thu, 2 Apr 2026 10:53:23 -0400
Subject: [PATCH] avoid deprecated ERR_load_crypto_strings API

This function was deprecated in 1.1.0 which setting OpenSSL API compt
to 0x10100000L disables the API.  Trying to build with OpenSSL 1.1.1w
fails now with:
ssherr-libcrypto.c:39:2: error:
  call to undeclared function 'ERR_load_crypto_strings';
  ISO C99 and later do not support implicit function declarations [-Wimplicit-function-declaration]
   39 |         ERR_load_crypto_strings();
      |         ^

From the OpenSSL CHANGES file:
> However, applications are strongly advised to compile their source files with
> -DOPENSSL_API_COMPAT=0x10100000L, which hides the declarations of all interfaces
> deprecated in 0.9.8, 1.0.0 or the 1.1.0 releases.

Switch to the replacement API which is available in OpenSSL 1.1.1 (the
oldest OpenSSH supports) and all current versions.
---
 configure.ac                                                 | 4 ++--
 regress/misc/ssh-verify-attestation/ssh-verify-attestation.c | 2 +-
 regress/unittests/sshsig/tests.c                             | 3 ++-
 regress/unittests/test_helper/test_helper.c                  | 2 +-
 ssherr-libcrypto.c                                           | 2 +-
 5 files changed, 7 insertions(+), 6 deletions(-)

NB: Deleted changes not needed for WASM build.  The configure hunk doesn't
affect the build in any way, and we don't compile or run any unnittest.

diff --git a/ssherr-libcrypto.c b/ssherr-libcrypto.c
index 5b817e54aa80..c825e1a66386 100644
--- a/ssherr-libcrypto.c
+++ b/ssherr-libcrypto.c
@@ -36,7 +36,7 @@ ssherr_libcrypto(void)
 	const char *reason = NULL, *file, *data;
 	int ln, fl;
 
-	ERR_load_crypto_strings();
+	OPENSSL_init_crypto(OPENSSL_INIT_LOAD_CRYPTO_STRINGS, NULL);
 	while ((e = ERR_get_error_line_data(&file, &ln, &data, &fl)) != 0) {
 		ERR_error_string_n(e, buf, sizeof(buf));
 		snprintf(msg, sizeof(msg), "%s:%s:%d:%s", buf, file, ln,
-- 
2.53.0.1185.g05d4b7b318-goog

